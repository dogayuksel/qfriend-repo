/* @flow weak */
const initialState = {
  appName: '',
  appVersion: '',
  firebase: '',
  sentryUrl: '',
  googleAnalyticsId: '',
};

const configReducer = (state = initialState) => state;

export default configReducer;
